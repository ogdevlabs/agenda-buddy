---
id: F-015-T04
feature: F-015
title: "Gateway failure translation, and proof the forwarded Host header doesn't break transport security"
status: planned
priority: null
labels: ["epic:api-gateway-and-mobile-contract", "user:ogdevlabs", "roadmap:F-015", "branch:feat/F-015-api-gateway-and-mobile-contract", backend, security]
depends_on: ["F-015-T03", "F-015-T05"]
acceptance_criteria: ["AC3 Given a client sends a request with a valid JWT through the gateway, when it reaches the destination service, then the JWT arrives unmodified (same claims, same signature) and the service validates it exactly as it would a direct call.", "AC4 Given an anonymous or invalid-JWT request through the gateway, when it reaches the destination, then it receives the same 401/403 a direct call would return.", "AC5 Given one backend service is stopped while the gateway and the other six remain up, when a client calls that service through the gateway, then the response identifies the failed service by name (failedService field, api-contracts.md §1), and requests to the other six succeed normally.", "[security] (T-303) Given a request routed through the gateway to any backend service, when that service's HSTS/redirect behavior is exercised, then it matches a direct call — no new redirect loop, no incorrect scheme in a Location header. 🧪 test-first"]
claimed_by: null
created: 2026-08-23
updated: 2026-08-23
---
[Created: 2026-08-23 | Author: ogdevlabs | Branch: feat/F-015-api-gateway-and-mobile-contract | Roadmap: F-015]
Two things, because both are about what crosses the gateway boundary unmodified: an error handler that attaches the failed destination's YARP cluster name to a ProblemDetails body on 5xx/timeout/unreachable (api-contracts.md §1) — this is the single point of translation PRD AC 5 needs, so MobileApp never has to guess which service failed from the route it called; and an explicit assertion (threat T-303) that YARP's default X-Forwarded-* header forwarding does not break any backend's UseAgendaBuddyTransportSecurity() → UseAuthentication() ordering or its HSTS/redirect behavior when reached through the gateway instead of directly.
Depends on F-015-T03 (the failure-translation handler needs the route/cluster table to exist first — it names clusters that table defines) and F-015-T05 (needs the gateway wired into the real AppHost graph to test against real services, not stubs).
**Reassigned during Build (wave-order fix):** AC3 and AC4 (JWT-passthrough proof) moved here from F-015-T05, which the Plan had assigned them to on the mistaken assumption that AppHost wiring alone makes a full request flow provable — it doesn't, without T03's route table. This task is the first point a full client→gateway→destination request can actually be exercised, so the auth-passthrough proof belongs here.
