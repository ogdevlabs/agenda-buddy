---
id: F-020-T02
feature: F-020
title: "Rename Kafka/Kafka.Tests to AgendaBuddy.Kafka/AgendaBuddy.Kafka.Tests"
status: planned
priority: null
labels: ["epic:api-refactor-rollout", "story:F-020-US-05", "backend", "user:ogdevlabs", "roadmap:F-020"]
depends_on: []
claimed_by: null
created: 2026-08-27
updated: 2026-08-27
---
PRD Requirements 17/18/20. ARCHITECTURE.md §10. Rename `Kafka/` → `AgendaBuddy.Kafka/`, `Kafka.Tests/` →
`AgendaBuddy.Kafka.Tests/`: folder, `.csproj`, solution entry, every `namespace Kafka.X;` declaration. `Kafka`
has zero `ProjectReference`s of its own (checked — it's a leaf project), so this task has no dependency on
`Library`'s rename and can run independently/first. Update the one known consumer, `EventAndCommands`'s
`ProjectReference` to `Kafka.csproj` and its `using Kafka.X`/`IKafkaClient` references (this task updates
that reference; `EventAndCommands`'s own rename is F-020-T03, which depends on this task completing first).
Also check `Booking.Core`/the 5 in-scope services for any direct `IKafkaClient`/`KafkaClient` reference
(Customer and Provider are the known ones per the Discover survey) and update their `using` statements.
Verify with `dotnet build agenda-buddy.sln`. AC 14/15/16.
