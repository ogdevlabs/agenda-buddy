---
id: F-020-T03
feature: F-020
title: "Rename EventAndCommands/EventsAndCommands.Tests to AgendaBuddy.EventAndCommands/AgendaBuddy.EventsAndCommands.Tests"
status: planned
priority: null
labels: ["epic:api-refactor-rollout", "story:F-020-US-05", "backend", "user:ogdevlabs", "roadmap:F-020"]
depends_on: [F-020-T01, F-020-T02]
claimed_by: null
created: 2026-08-27
updated: 2026-08-27
---
PRD Requirements 17/18/20/21. ARCHITECTURE.md §10. Rename `EventAndCommands/` →
`AgendaBuddy.EventAndCommands/`, `EventsAndCommands.Tests/` → `AgendaBuddy.EventsAndCommands.Tests/` —
**the existing `Event`/`Events` naming inconsistency between the two folder names is preserved, not fixed**
(PRD Requirement 21/Out of Scope: this feature renames, it doesn't also unify unrelated pre-existing naming
bugs). Update `EventAndCommands.csproj`'s two `ProjectReference`s (`Kafka.csproj`→
`AgendaBuddy.Kafka.csproj`, `Library.csproj`→`AgendaBuddy.Library.csproj`, both from prior tasks) and every
`namespace EventAndCommands.X;`/`using EventAndCommands.X` reference across the solution — every remaining
service (the 5 not yet migrated in this feature, plus Identity if it references `IEventStore` directly) and
`AgendaBuddy.IntegrationTests`'s `Audit/EventStoreWriteGuardTest.cs` (its `ScanRoots` array references
`EventAndCommands` by string path — update it here, not later). Depends on T01 (Library) and T02 (Kafka)
since `EventAndCommands.csproj` references both directly. Verify with `dotnet build agenda-buddy.sln` and a
full integration-suite run (not just a build — `EventStoreWriteGuardTest` itself needs to still find every
handler file after the path string changes). AC 6/14/15/16.
