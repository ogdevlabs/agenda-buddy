---
id: F-020-T12
feature: F-020
title: "Migrate Customer to AgendaBuddy.Customer.Api/Core/Domain/Infrastructure (Clean Architecture split, largest + Kafka fix)"
status: done
priority: null
labels: ["epic:api-refactor-rollout", "story:F-020-US-01,F-020-US-02,F-020-US-04", "backend", "user:ogdevlabs", "roadmap:F-020"]
depends_on: [F-020-T01, F-020-T02, F-020-T03]
claimed_by: null
created: 2026-08-27
updated: 2026-08-27
---
PRD Requirements 1-13, threat T-204. ARCHITECTURE.md §1-§9/§9 open item. **Largest and highest-risk of the 5
service migrations — 10 routes, 4 existing handler files, an unexplained `MessageRequest.cs`, and the one
remaining dormant `IKafkaClient` downcast bug (`agenda-buddy-5og`'s class of defect, never fixed for
Customer specifically). Do this last, after the other 4 services are done, so any newly-discovered
migration defect has already been caught and fixed once on a simpler service first.**

Before scaffolding: confirm what `Customer/Requests/MessageRequest.cs` actually wires to today (a handler
file, an inline check, or dead code) — needed to size the migration accurately.

Scaffold `AgendaBuddy.Customer.Api` (was `Customer`), `AgendaBuddy.Customer.Core`,
`AgendaBuddy.Customer.Domain`, `AgendaBuddy.Customer.Infrastructure`. Delete
`Customer/Requests/RequestCollection.cs`/`IRequestCollection.cs`. Real `mediator.Send` dispatch,
`DataResponse<T>`, `GuardClauses`, interface-typed handler constructors verified against DI registration
(depends on F-020-T01/T03), real Moq-based tests (no stubs), `EventStoreWriteGuardTest` `ScanRoots`
addition, `CacheAside` read-site check, `CustomerRouteContractTest.cs` unchanged, live `DataResponse<T>`
verification per route, `docs/api/openapi/Customer.json` regeneration.

**Fix threat T-204 as part of this task, not a follow-up**: `AddCustomerCommandHandler`'s constructor is
currently typed to the concrete `KafkaClient` class, only safe today because `RequestCollection` hand-casts
`(kafkaClient as KafkaClient)!` from the `IKafkaClient` DI registration. Retype to `IKafkaClient?` (matching
Booking's own F-019 fix for the identical bug shape) as part of moving this handler to real MediatR
dispatch — depends on F-020-T02 (Kafka rename) landing first so the reference resolves cleanly. AC 1-13.
