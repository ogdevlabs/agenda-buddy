---
id: F-020-T04
feature: F-020
title: "Rename Gateway to AgendaBuddy.Gateway"
status: planned
priority: null
labels: ["epic:api-refactor-rollout", "story:F-020-US-05", "backend,devops", "user:ogdevlabs", "roadmap:F-020"]
depends_on: []
claimed_by: null
created: 2026-08-27
updated: 2026-08-27
---
PRD Requirements 17/18/20. ARCHITECTURE.md §10. Rename `Gateway/` → `AgendaBuddy.Gateway/`: folder, `.csproj`,
solution entry, every `namespace Gateway.X;` declaration (`Gateway/Program.cs`,
`Gateway/AspireServiceDiscoveryProxyConfigProvider.cs`, `Gateway/GatewayAnchor.cs`). `Gateway.csproj` only
references `AgendaBuddy.ServiceDefaults` (already correctly named) — no dependency on `Library`'s rename,
so this task can run independently, even first. Update `AgendaBuddy.AppHost`'s project reference and its
`WithReference`/`WaitFor` calls for the Gateway resource, and `AgendaBuddy.IntegrationTests/GlobalUsings.cs`'s
`GatewayAnchor = Gateway.GatewayAnchor` alias (this is the one anchor with no `MongoDbConfiguration` to
reuse, per the existing comment — update the alias's right-hand side to `AgendaBuddy.Gateway.GatewayAnchor`).
Also check `.github/workflows/dotnet.yml`'s Gateway path filters (the "Gateway has zero CI coverage" gap
recorded in F-017's tech debt is a separate, unrelated issue — do not fix it here, just make sure whatever
filters DO exist still resolve to the new path). Verify with `dotnet build agenda-buddy.sln` and a live
AppHost health check (Gateway's `/health` must still resolve). AC 14/15/16/17.
