---
id: F-020-T13
feature: F-020
title: "Final verification — full solution build, full test suite, live AppHost smoke test, blast-radius check, verification.md"
status: planned
priority: null
labels: ["epic:api-refactor-rollout", "story:F-020-US-01,F-020-US-02,F-020-US-03,F-020-US-05,F-020-US-06", "backend,devops", "user:ogdevlabs", "roadmap:F-020"]
depends_on: [F-020-T01, F-020-T02, F-020-T03, F-020-T04, F-020-T05, F-020-T06, F-020-T07, F-020-T08, F-020-T09, F-020-T10, F-020-T11, F-020-T12]
claimed_by: null
created: 2026-08-27
updated: 2026-08-27
---
PRD all ACs. ARCHITECTURE.md §0/§10. The closing task — attest every PRD acceptance criterion with
evidence, same discipline as F-019-T11:

1. `dotnet build agenda-buddy.sln` (full solution, not the backend slnf — the rename touches `MobileApp`/
   `AgendaBuddy.IntegrationTests` too) — 0 errors.
2. `grep -oP` the solution file: exactly 30 project entries, every one prefixed `AgendaBuddy.` (AC 14).
3. Solution-wide grep for any remaining unprefixed `namespace`/`using` reference to any of the 25 renamed
   old names — zero matches anywhere (AC 15).
4. Full test suites: backend (`agenda-buddy-backend.slnf`), integration
   (`AgendaBuddy.IntegrationTests/AgendaBuddy.IntegrationTests.csproj`), mobile
   (`AgendaBuddy.MobileApp.Tests/AgendaBuddy.MobileApp.Tests.csproj /p:MobileWorkloads=false`) — 0 failures,
   compare against the pre-feature baseline (991 tests after F-019) and disclose the actual delta, don't
   assume it matches a prediction.
5. `dotnet format agenda-buddy-backend.slnf --verify-no-changes` clean (AC 12).
6. `docs/api/openapi/*.json` regenerated for all 7 renamed/migrated services (Booking, Calendar, Customer,
   Provider, Services, Profession — Identity's spec doesn't change route-wise, but its file name/title do
   from the rename) — `OpenApiSpecDriftTest` passing with zero manual edits (AC 18).
7. `.github/workflows/dotnet.yml`, `scripts/generate-openapi.sh`, `scripts/run-ios.sh` — zero remaining
   old-name references (AC 19).
8. Live AppHost smoke test covering BOTH workstreams: all 8 processes reach `/health`=`Healthy` (AC 17);
   for each of the 5 migrated services, at least one real HTTP round trip through the Gateway confirming
   the `DataResponse<T>` envelope on a real success response (AC 4) — same bar F-019's own Ship-gate smoke
   test set, not skipped this time either given the scale of this feature's production surface.
9. `git diff main --name-only` blast-radius check — every changed file accounted for as either a
   CQRS-migration touch (5 services) or a rename-only touch (the other 20 projects); no unexplained file.
10. Write `docs/pdlc/design/api-refactor-rollout/verification.md` attesting all 23 PRD ACs with evidence
    (grep output for structural ACs, real test/HTTP evidence for behavioral ones), same structure as
    F-019's own `verification.md`. Disclose, don't paper over, any deviation found (matches this project's
    standing culture — every prior feature's real defects were found by running the software, not by
    review alone; expect this one to be no different at this scale).
