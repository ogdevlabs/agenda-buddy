---
id: F-020-T08
feature: F-020
title: "Migrate Calendar to AgendaBuddy.Calendar.Api/Core/Domain/Infrastructure (Clean Architecture split)"
status: planned
priority: null
labels: ["epic:api-refactor-rollout", "story:F-020-US-01,F-020-US-02,F-020-US-04", "backend", "user:ogdevlabs", "roadmap:F-020"]
depends_on: [F-020-T01, F-020-T03]
claimed_by: null
created: 2026-08-27
updated: 2026-08-27
---
PRD Requirements 1-13. ARCHITECTURE.md §1-§9. **Simplest migration in the batch — 2 routes, 2 handlers, no
Kafka, no extra request types (Discover survey).** Scaffold `AgendaBuddy.Calendar.Api` (was `Calendar`,
thin — endpoints/DI only), `AgendaBuddy.Calendar.Core` (MediatR handlers, moved from
`AgendaBuddy.EventAndCommands/Commands/Calendar` and `/Queries/Calendar`), `AgendaBuddy.Calendar.Domain`
(commands/queries, its own `DataResponse<T>`), `AgendaBuddy.Calendar.Infrastructure` (thin/likely empty —
YAGNI, don't pad it). Delete `Calendar/Requests/RequestCollection.cs`/`IRequestCollection.cs`. Both routes
dispatch via real `mediator.Send(command, ct)` with the real `CancellationToken`. Type each handler's
constructor against `Library.Services.ICalendarService` (now `AgendaBuddy.Library.Services.*`) if it
already covers everything the handler calls — check DI registration in `ServiceCollectionExtension.cs`
after retyping (F-019 Party Review lesson: this silently breaks DI if the interface isn't also registered).
Use `GuardClauses` for null/argument checks. Add real Moq-based unit tests for every handler's success/
failure/audit-write paths — no placeholder stubs. Add `AgendaBuddy.Calendar.Core` to
`EventStoreWriteGuardTest`'s `ScanRoots`. Confirm whether any `CacheAside.GetOrCreateAsync` read site exists
in Calendar that must survive untouched. Verify `CalendarRouteContractTest.cs` passes with zero status-code
changes, and every route returns `DataResponse<T>` on success via a real HTTP request (not just unit tests).
Regenerate `docs/api/openapi/Calendar.json`. AC 1-13.
