---
id: F-020-T11
feature: F-020
title: "Migrate Provider to AgendaBuddy.Provider.Api/Core/Domain/Infrastructure (Clean Architecture split)"
status: done
priority: null
labels: ["epic:api-refactor-rollout", "story:F-020-US-01,F-020-US-02,F-020-US-04", "backend", "user:ogdevlabs", "roadmap:F-020"]
depends_on: [F-020-T01, F-020-T03]
claimed_by: null
created: 2026-08-27
updated: 2026-08-27
---
PRD Requirements 1-13. ARCHITECTURE.md §1-§9. **6 routes, 6 handlers — the largest of the 4
already-Kafka-clean services.** Scaffold `AgendaBuddy.Provider.Api` (was `Provider`),
`AgendaBuddy.Provider.Core`, `AgendaBuddy.Provider.Domain`, `AgendaBuddy.Provider.Infrastructure`. Same
treatment as F-020-T08: delete `RequestCollection`/`IRequestCollection`, real `mediator.Send` dispatch,
`DataResponse<T>`, `GuardClauses`, interface-typed handler constructors verified against DI registration,
real Moq-based tests (no stubs), `EventStoreWriteGuardTest` `ScanRoots` addition, `CacheAside` read-site
check, `ProviderRouteContractTest.cs` unchanged, live `DataResponse<T>` verification per route,
`docs/api/openapi/Provider.json` regeneration. Provider's `IKafkaClient` downcast was already fixed at
F-018 (`agenda-buddy-5og`) — confirm the moved handler keeps the `IKafkaClient?` typing, not a regression
back to the concrete `KafkaClient` class. AC-11 (image-pull diagnostics) and F-016's `AssertRole`/
`AssertOwner` dual-check on provider creation (account-takeover-by-registration guard) are unrelated,
pre-existing controls — confirm they survive the handler move unchanged, don't re-derive or "improve" them.
AC 1-13.
