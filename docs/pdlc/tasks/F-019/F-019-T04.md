---
id: F-019-T04
feature: F-019
title: "Rewrite Booking.Api: delete RequestCollection/IRequestCollection, dispatch the 3 original routes via mediator.Send, apply DataResponse<T>"
status: open
priority: null
labels: ["epic:api-refactor-pilot-booking", "user:ogdevlabs", "roadmap:F-019", "branch:feat/F-019-api-refactor-pilot-booking", "backend"]
depends_on: [F-019-T03]
claimed_by: null
created: 2026-08-26
updated: 2026-08-26
---
PRD Requirements 2, 3, 4, 5, 9, 10. ARCHITECTURE.md §2/§4. Rename/repurpose `Booking` → `Booking.Api`. Delete `Booking/Requests/RequestCollection.cs` and `IRequestCollection.cs` outright — no replacement type. The 3 original routes' endpoint bodies become `await mediator.Send(command, ct)`, using the real request `CancellationToken` (not `new CancellationToken()`). Wrap success responses in `DataResponse<T>`. This is also where `agenda-buddy-5og` (the dormant `IKafkaClient` downcast) disappears — confirm with a `git grep "as KafkaClient"` returning zero matches after this task, per PRD AC4.
