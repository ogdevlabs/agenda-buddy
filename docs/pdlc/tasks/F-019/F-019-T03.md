---
id: F-019-T03
feature: F-019
title: Scaffold Booking.Domain + Booking.Core + Booking.Infrastructure; migrate the 3 original routes' commands/handlers
status: open
priority: null
labels: ["epic:api-refactor-pilot-booking", "user:ogdevlabs", "roadmap:F-019", "branch:feat/F-019-api-refactor-pilot-booking", "backend"]
depends_on: [F-019-T01]
claimed_by: null
created: 2026-08-26
updated: 2026-08-26
---
PRD Requirements 1-4, 7-9. ARCHITECTURE.md §1/§2/§6. Create `Booking.Domain` (commands, queries, request/response DTOs, `DataResponse<T>` per T01's confirmed shape), `Booking.Core` (command/query handlers, moved from `EventAndCommands/Commands/Booking`), `Booking.Infrastructure` (thin — only if a real Booking-specific repository need exists; do not pad it for layout symmetry, per ARCHITECTURE §6's YAGNI note). Migrate the 3 original routes' commands/handlers (Book/Update/Cancel) into the new projects — `Library.Services.BookingService` calls stay exactly as they are (Library is unchanged). Does NOT touch `Booking.Api`/`Program.cs` yet — that's T04.
