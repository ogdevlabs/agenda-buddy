---
id: F-025-T02
feature: F-025
title: Future-dating check in BookingAppointmentCommandHandler
status: done
---

Registered `IDateTimeProvider`/`SystemDateTimeProvider` as a singleton in
`AgendaBuddy.Booking.Api/Extensions/ServiceCollectionExtension.cs` (mirrors
`AgendaBuddy.Identity/Program.cs`), injected it into `BookingAppointmentCommandHandler`, and rejects
`Start <= dateTimeProvider.UtcNow` with `Result.Fail` before the provider lookup.

**Tests:** `BookingCorrectnessTest.PostAppointments_StartInThePast_Returns400` (integration, real Mongo).
