---
id: F-025-T04
feature: F-025
title: Descope AppointmentStatus.Cancelled soft-delete decision
status: done
---

The feature record also named the dead `AppointmentStatus.Cancelled` enum value (cancellation
hard-deletes instead of transitioning). Descoped to its own record — filed as `agenda-buddy-m6m` — at
Design, rather than bundled into this fix. Reasoning: `docs/pdlc/design/booking-correctness/ARCHITECTURE.md`
§4.

No code change for this task; it is the scoping decision itself, recorded so `_feature.md` doesn't read
as though that half was silently dropped.
