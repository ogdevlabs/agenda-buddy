---
id: F-025-T01
feature: F-025
title: Start < End validation at the API boundary
status: done
---

Added `.Rule(m => m.Start < m.End).WithMessage(...)` to
`AgendaBuddy.Booking.Api/Validation/AppointmentEntitySpecification.cs`. Stateless, so it belongs in the
existing Validot spec already wired to `POST /appointments`, not in the handler.

**Tests:** `AppointmentEntitySpecificationTest.Validate_EndBeforeStart_AnyErrors`,
`Validate_EndEqualsStart_AnyErrors`.
