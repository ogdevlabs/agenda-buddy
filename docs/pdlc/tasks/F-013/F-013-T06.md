---
id: F-013-T06
feature: F-013
title: "Update CI: path filters, AppHost build, guard assertions (R-8)"
status: open
priority: 3
labels: [ci, "risk:R-8"]
depends_on: [F-013-T02, F-013-T08]
claimed_by: null
created: 2026-08-15
updated: 2026-08-17
---
Without this the feature merges with ZERO jobs run - the dorny/paths-filter currently ignores global.json, Dockerfile*, docker-compose*, and .github/**. Extend the api filter with AgendaBuddy.AppHost/**, AgendaBuddy.ServiceDefaults/**, global.json, Directory.Build.props, Dockerfile*, */Dockerfile, docker-compose*.yml, .github/workflows/**. Add: AppHost build step; assertion that AppHost has no MobileApp reference (AC-1.5); assertion that no mongodb+srv URI with a password exists in tracked files (AC-2.1/2.2). Verify jobs actually run on the PR. NOTE: this is not the CONSTITUTION section 7 mandatory security scan - that stays open, deferred to F-017.

## Standup amendments (Wave 2 kickoff, 2026-08-17)

- **Now depends on T-08**, not just T-02: the `dotnet build AgendaBuddy.AppHost/...` step targets a project T-08 creates, so landing right after T-02 would fail on a missing path.
- **Do not add `dotnet workload install aspire`** — no such workload exists (T-01: `dotnet workload search aspire` returns empty; the AppHost builds on a bare SDK 10.0.400 via `Aspire.AppHost.Sdk` 13.4.6).
- `.github/workflows/dotnet.yml:48` already includes `'*.sln'` in the `api` filter, so the solution edit alone flips `api=true`; the new dirs still need adding.
- Keep the `/p:MobileWorkloads=false` build step **scoped to the AppHost project**. The solution-wide `build-and-test` step (`:83-87`) already passes that flag and already tolerates a pre-existing `MobileApp` compile failure (CS0103, `MobileApp/ViewModels/CustomersViewModel.cs:125`) — do not write a guard that would attribute that defect to this feature.
