---
id: F-013-T04
feature: F-013
title: Add connection-resolution tests for all 7 services (R-3)
status: done
priority: 2
labels: [test]
depends_on: [F-013-T03]
claimed_by: null
created: 2026-08-15
updated: 2026-08-17
---
The refactored code has ZERO existing coverage - no test exists for any ServiceCollectionExtension. Add NEW tests only (AC-5.2 forbids editing existing tests). Per service assert: resolves from ConnectionStrings:mongodb; resolves from each legacy fallback; throws with a message naming the keys when absent. Plus a KafkaClient test for AC-5.5 (config-driven BootstrapServers, localhost:9092 default).

## Standup amendments (Wave 2 kickoff, 2026-08-17)

- Ownership split: T-03 owns the resolver/health-check unit tests in `Library.Tests`, including the named-key exception message. **T-04 owns the per-service resolution tests and the new Kafka test.** The previous double-claim is resolved.
- The Kafka test must be a **new** method/file — `Kafka.Tests/KafkaClientTest.cs:8-11` is an empty `METHOD(){}` stub and AC-5.2 forbids editing existing tests. Requires T-07 to expose `BootstrapServers` as `internal` + `[InternalsVisibleTo("Kafka.Tests")]`.
- Reuse the existing `Mock<IConfiguration>` idiom from the service `MongoDbConfigurationTest` files rather than inventing a per-service helper (11-testing.md:97 already flags near-duplicate test debt).
- Where a test exercises `MongoDbConfiguration`, inject a real `MongoClient`, not a mock — `MongoClient()` casts to the concrete type (ARCHITECTURE.md 3.3).
