---
id: F-013-T07
feature: F-013
title: Make KafkaClient.BootstrapServers configuration-driven (AC-5.5)
status: open
priority: 3
labels: [infra]
depends_on: [F-013-T01]
claimed_by: null
created: 2026-08-15
updated: 2026-08-15
---
Kafka/KafkaClient.cs:12 hardcodes localhost:9092 - CONSTITUTION section 9 flags this as blocking non-local deployment. Add an optional IConfiguration ctor param reading ConnectionStrings:kafka then Kafka:BootstrapServers, defaulting to localhost:9092. Optional param keeps new KafkaClient() valid so Kafka.Tests/KafkaClientTest.cs and the AddSingleton registrations still work unchanged. Do NOT fix the pre-existing defects: topic-name collision across email domains, already-exists-treated-as-failure, unguarded e.Results[0], per-call AdminClient.

## Standup amendments (Wave 2 kickoff, 2026-08-17)

- **This is not "add an optional ctor param."** The hardcoded `localhost:9092` is a local `AdminClientConfig` initializer inside `CreateTopicIfNotExist` (`Kafka/KafkaClient.cs:8-13`), not a class member. Convert to a primary-constructor class with a `BootstrapServers` member and rewire the local `AdminClientConfig` to read it.
- Expose it as `internal` with `[InternalsVisibleTo("Kafka.Tests")]` — if it stays `private`, T-04 cannot assert the resolved value without a live broker.
- Keep `new KafkaClient()` valid so the existing registrations and `Kafka.Tests` still compile (AC-5.2).
- Hold the boundary: do **not** fix the topic-name collision, the already-exists-as-failure branch, the unguarded `e.Results[0]`, or the per-call `AdminClient` — easy to blur while already editing this 44-line file.
