meta {
  name: 2 Get customer by email
  type: http
  seq: 2
}
get {
  url: {{gatewayUrl}}/api/v1/customers/{{customerEmail}}
}
docs {
  Authenticated but **not** role-gated — a customer legitimately reads their own record through here.
  Only the *list* route requires the `Provider` role.
}
