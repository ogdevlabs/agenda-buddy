name: Deploy to Azure Container Apps

# Manual only, deliberately. A push-triggered deploy on a project with one shared Atlas cluster and
# no staging/production split is a foot-gun; wire this to `push: branches: [main]` once
# docs/deployment.md's "Before this is production" list is actually done.
#
# No step in this workflow requires a human to run a CLI command by hand: Terraform provisions
# this environment's resource group, deploy identity and secrets store, then azd/Aspire's own
# publisher provisions and deploys the application resource graph — both non-interactively,
# both authenticated via the same GitHub OIDC federated credential. The only human action left
# is the one-time-per-subscription infra/terraform/bootstrap apply (see docs/deployment.md) and
# the one-time-per-new-environment apply that creates that environment's own deploy identity
# (chicken-and-egg: an identity that doesn't exist yet can't authenticate the run that creates
# it — see docs/deployment.md's "One-time subscription bootstrap" section).
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment name (matches the GitHub Environment and the azd environment name, e.g. staging)"
        required: true
        default: staging
      provision:
        description: "Run azd provision (infrastructure) as well as deploy. REQUIRED on an environment's first ever run — azd deploy has nothing to deploy to until provision has created it."
        type: boolean
        required: false
        default: false

# Federated credentials, not a stored client secret: id-token lets both terraform and azd
# exchange a GitHub OIDC token for an Azure one, so there is no long-lived credential in this
# repository to leak or rotate.
permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-${{ inputs.environment }}
  cancel-in-progress: false

env:
  ARM_USE_OIDC: "true"
  ARM_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
  ARM_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}

jobs:
  deploy:
    runs-on: ubuntu-24.04
    environment: ${{ inputs.environment }}

    steps:
      - uses: actions/checkout@v7

      # Fail fast and name every missing value. The "Assert every service starts in Development"
      # guard in dotnet.yml silently consumed two secrets that were never created and only failed
      # months later, on the first PR that ran it — a preflight is cheap insurance against repeating
      # that with a half-provisioned Azure environment.
      - name: Preflight — required configuration is present
        env:
          AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
          TF_STATE_RESOURCE_GROUP: ${{ vars.TF_STATE_RESOURCE_GROUP }}
          TF_STATE_STORAGE_ACCOUNT: ${{ vars.TF_STATE_STORAGE_ACCOUNT }}
          TF_STATE_CONTAINER: ${{ vars.TF_STATE_CONTAINER }}
          TF_VAR_agenda_buddy_connection_string: ${{ secrets.ATLAS_AGENDA_BUDDY_CONNECTION_STRING }}
          TF_VAR_identity_db_connection_string: ${{ secrets.ATLAS_IDENTITY_DB_CONNECTION_STRING }}
          TF_VAR_resend_api_key: ${{ secrets.RESEND_API_KEY }}
          TF_VAR_jwt_public_key: ${{ secrets.JWT_PUBLIC_KEY }}
          TF_VAR_jwt_private_key: ${{ secrets.JWT_PRIVATE_KEY }}
        run: |
          missing=""
          for name in AZURE_CLIENT_ID AZURE_TENANT_ID AZURE_SUBSCRIPTION_ID AZURE_LOCATION \
                      TF_STATE_RESOURCE_GROUP TF_STATE_STORAGE_ACCOUNT TF_STATE_CONTAINER; do
            [ -n "${!name:-}" ] || missing="$missing $name (repository or environment variable)"
          done
          for name in TF_VAR_agenda_buddy_connection_string TF_VAR_identity_db_connection_string \
                      TF_VAR_jwt_public_key TF_VAR_jwt_private_key; do
            [ -n "${!name:-}" ] || missing="$missing $name (environment secret)"
          done
          if [ -n "$missing" ]; then
            echo "::error::Missing required configuration:$missing — see docs/deployment.md"
            exit 1
          fi
          echo "All required configuration present."

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~> 1.9"

      # This environment's own deploy identity (client_id/tenant_id/subscription_id) is what
      # authenticates this very step via OIDC — for a brand-new environment that identity does
      # not exist yet, which is why creating one is a one-time human-run `terraform apply` (see
      # docs/deployment.md) rather than something this workflow can do for itself. Every run
      # after that first one is unattended.
      - name: Terraform init
        working-directory: infra/terraform/environment
        run: |
          terraform init -input=false \
            -backend-config="resource_group_name=${{ vars.TF_STATE_RESOURCE_GROUP }}" \
            -backend-config="storage_account_name=${{ vars.TF_STATE_STORAGE_ACCOUNT }}" \
            -backend-config="container_name=${{ vars.TF_STATE_CONTAINER }}" \
            -backend-config="key=agenda-buddy-${{ inputs.environment }}.tfstate"

      - name: Terraform apply
        working-directory: infra/terraform/environment
        env:
          TF_VAR_environment_name: ${{ inputs.environment }}
          TF_VAR_location: ${{ vars.AZURE_LOCATION }}
          # The state account is also a variable, not only backend config: the config grants the deploy
          # identity a blob role on it, and a role assignment cannot reference backend settings.
          TF_VAR_state_resource_group_name: ${{ vars.TF_STATE_RESOURCE_GROUP }}
          TF_VAR_state_storage_account_name: ${{ vars.TF_STATE_STORAGE_ACCOUNT }}
          TF_VAR_subscription_id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_tenant_id: ${{ vars.AZURE_TENANT_ID }}
          TF_VAR_agenda_buddy_connection_string: ${{ secrets.ATLAS_AGENDA_BUDDY_CONNECTION_STRING }}
          TF_VAR_identity_db_connection_string: ${{ secrets.ATLAS_IDENTITY_DB_CONNECTION_STRING }}
          TF_VAR_resend_api_key: ${{ secrets.RESEND_API_KEY }}
          TF_VAR_jwt_public_key: ${{ secrets.JWT_PUBLIC_KEY }}
          TF_VAR_jwt_private_key: ${{ secrets.JWT_PRIVATE_KEY }}
        run: terraform apply -input=false -auto-approve

      # Feeds azd exactly what it would otherwise have asked a human for interactively:
      # AZURE_CLIENT_ID/TENANT_ID/SUBSCRIPTION_ID for federated login, plus the connection
      # strings and JWT keys the AppHost's cloud shape (AppHostWiring.cs) declares as parameters
      # — read from this environment's own Key Vault rather than a hand-maintained secret blob.
      - name: Read Terraform outputs
        id: tf
        working-directory: infra/terraform/environment
        run: |
          {
            echo "client_id=$(terraform output -raw client_id)"
            echo "resource_group_name=$(terraform output -raw resource_group_name)"
            echo "key_vault_name=$(terraform output -raw key_vault_name)"
          } >> "$GITHUB_OUTPUT"

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      # Must precede any azd invocation: azd is not present on the runner image, so calling
      # `azd auth login` before this step fails with command not found.
      - name: Install azd
        uses: Azure/setup-azd@v2

      # Two separate credential stores, both needed. `azd auth login` authenticates azd only; the
      # Key Vault step below shells out to the az CLI, which reads none of azd's state and would
      # fail unauthenticated without this.
      - name: az login (federated, this environment's own identity)
        uses: azure/login@v2
        with:
          client-id: ${{ steps.tf.outputs.client_id }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: azd login (federated, this environment's own identity)
        run: |
          azd auth login \
            --client-id "${{ steps.tf.outputs.client_id }}" \
            --tenant-id "${{ vars.AZURE_TENANT_ID }}" \
            --federated-credential-provider github

      - name: Configure azd environment from Key Vault
        env:
          KEY_VAULT_NAME: ${{ steps.tf.outputs.key_vault_name }}
          # azd's generated Bicep hardcodes the group name as rg-<azd env name>, and
          # AZURE_RESOURCE_GROUP does not override it. Prefixing here keeps the application in
          # rg-agenda-buddy-<env>, the group Terraform already owns, rather than a separate rg-<env>.
          # The GITHUB Environment stays unprefixed: its name is baked into the OIDC subject.
          AZD_ENV: agenda-buddy-${{ inputs.environment }}
        run: |
          set -eu
          azd env new "$AZD_ENV" \
            --location "${{ vars.AZURE_LOCATION }}" \
            --subscription "${{ vars.AZURE_SUBSCRIPTION_ID }}" \
            --no-prompt || azd env select "$AZD_ENV"

          # Written straight into azd's own config file rather than through `azd env set` or
          # `azd env config set`. Both of those mangle a multiline value: `env set` stores the PEM
          # unescaped and azd then fails with "invalid character '\n' in string literal" building the
          # Bicep parameters file, while `config set` silently stores nothing at all. Writing the JSON
          # ourselves escapes newlines correctly, which matters because two of these five values are
          # PEM keys.
          #
          # Key names are the Aspire parameter names, NOT the AZURE_-prefixed environment-variable
          # form, and they keep their original casing (IdentityDb, not identity_db). Read them off
          # `azd provision --preview --no-prompt`, which lists every parameter with both spellings.
          python3 - <<'PY'
          import json, os, pathlib, subprocess, sys

          vault = os.environ["KEY_VAULT_NAME"]
          env_name = os.environ["AZD_ENV"]

          # Key Vault secret name -> azd parameter name. resend-api-key is absent when the
          # environment has no mail provider, which is supported: the service warns at startup.
          wanted = {
              "agenda-buddy-connection": ("agenda_buddy", True),
              "identity-db-connection": ("IdentityDb", True),
              "jwt-public-key": ("jwt_public_key", True),
              "jwt-private-key": ("jwt_private_key", True),
              "resend-api-key": ("resend_api_key", False),
          }

          parameters = {}
          for secret, (param, required) in wanted.items():
              result = subprocess.run(
                  ["az", "keyvault", "secret", "show", "--vault-name", vault,
                   "--name", secret, "--query", "value", "-o", "tsv"],
                  capture_output=True, text=True)
              if result.returncode != 0:
                  if required:
                      print(f"::error::{secret} is missing from {vault}.")
                      sys.exit(1)
                  print(f"::warning::{secret} is not set in {vault} - continuing without it.")
                  continue
              # az strips no trailing newline of its own, but tsv output appends one.
              value = result.stdout.rstrip("\n")

              # azd writes each parameter into a per-service main.bicepparam as a single-line
              # string literal, and Bicep rejects a raw newline inside one ("BCP004: The string at
              # this location is not terminated"). The two PEM keys are the only multiline values.
              # Escaped here and un-escaped by the services that read them: both
              # AuthenticationExtensions and IdentityService.ReadPrivateKeyPem already call
              # .Replace("\\n", "\n") for exactly this case.
              parameters[param] = value.replace("\n", "\\n")

          path = pathlib.Path(".azure") / env_name / "config.json"
          config = json.loads(path.read_text()) if path.exists() and path.stat().st_size else {}
          config.setdefault("infra", {}).setdefault("parameters", {}).update(parameters)
          path.write_text(json.dumps(config, indent=2))
          print(f"wrote {len(parameters)} parameters to {path}: {sorted(parameters)}")
          PY

      - name: azd provision
        if: ${{ inputs.provision }}
        run: azd provision --no-prompt

      - name: azd deploy
        run: azd deploy --no-prompt

      - name: Smoke test — the gateway reports healthy
        if: success()
        env:
          RESOURCE_GROUP: ${{ steps.tf.outputs.resource_group_name }}
        run: |
          set -eu

          # Asked of Azure directly, not of `azd show`. azd show --output json reports each service as
          # {project, target:{resourceIds:[]}} with no endpoint field at all, so the previous version of
          # this step could never have passed -- it failed with "azd show reported no endpoint for the
          # gateway service" on the first deploy that actually got this far.
          #
          # Only the gateway has external ingress (AppHostWiring.cs keeps the seven domain services
          # internal-only), so it is the one endpoint a runner can reach; a service being unreachable
          # from here is the design, not a fault.
          fqdn=$(az containerapp show --name gateway --resource-group "$RESOURCE_GROUP" \
                   --query "properties.configuration.ingress.fqdn" -o tsv)

          if [ -z "$fqdn" ]; then
            echo "::error::gateway has no external ingress FQDN in $RESOURCE_GROUP."
            exit 1
          fi
          echo "gateway: https://$fqdn"

          # A just-deployed revision can take a few seconds to start serving, so each probe is retried
          # rather than failing the whole deploy on a cold start.
          for probe in /health /alive; do
            ok=""
            for attempt in 1 2 3 4 5 6; do
              code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 20 "https://$fqdn$probe" || echo 000)
              if [ "$code" = "200" ]; then
                echo "  $probe -> 200"
                ok=1
                break
              fi
              echo "  $probe -> $code (attempt $attempt/6)"
              sleep 10
            done
            [ -n "$ok" ] || { echo "::error::https://$fqdn$probe never returned 200."; exit 1; }
          done

          echo "Gateway healthy: both /health and /alive returned 200 over HTTPS."
